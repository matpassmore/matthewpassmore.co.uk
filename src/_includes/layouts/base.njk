<!DOCTYPE html>
<html lang="en" class="no-js">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">

    {% set pageTitle = title + ' - ' + site.name %}
    {% set pageDesc = '' %}
    {% set siteTitle = site.name %}
    {% set currentUrl = site.url + page.url %}

    {% if metaTitle %}
      {% set pageTitle = metaTitle %}
    {% endif %}

    {% if metaDesc %}
      {% set pageDesc = metaDesc %}
    {% endif %}

    <title>{{ pageTitle }}</title>

    <link rel="alternate" href="/feed/writing.xml" title="Matthew Passmore's writing" type="application/atom+xml">

    <link href="https://fonts.googleapis.com/css?family=Crimson+Pro:400i,500,600,800|Source+Sans+Pro:400,400i,600,700&display=swap" rel="stylesheet">
    <style>{% include 'css/main.css' %}</style>

    <!-- DETECT JS -->
    <script>document.documentElement.className = document.documentElement.className.replace("no-js","js");</script>
  </head>

  <body class="pt-500" data-controller="{{ controller }}" data-action="{{ action }}">
    {% include "partials/icon-sprite.njk" %}

    <div class="o-wrapper">
    
      <a href="#main">Skip to content</a>

      <header class="c-siteheader">

        <div>

          <!-- BRANDING -->
          <div class="c-siteheader__brand">
            <p class="text-600 font-serif leading-none"><a href="/">Matthew Passmore</a></p>
            <p>Website strategy &amp; development</p>
          </div><!-- .c-siteheader__brand -->
          
          
          <!-- NAVIGATION -->
          <nav class="c-siteheader__nav">
            <ul class="list-reset">
              <!--
              <li><a href="#about">About</a></li>
              -->
              <li><a href="mailto:{{ site.email }}">Contact</a></li>
              <li><a href="/writing/">Writing</a></li>
              <li class="[ c-siteheader__cta ] [ font-semibold font-serif text-500 ]">
                <a class="c-underline" href="mailto:{{ site.email }}">
                  Let's talk
                  <svg class="text-hotpink" width="23" height="23" aria-hidden="true"><use xlink:href="#icon-chat"></use></svg>
                </a>
              </li>
            </ul>
          </nav>

        </div>

      </header><!-- .c-siteheader -->


      <!-- MAIN CONTENT -->
      <main>
        {% block content %}
        {{ content | safe }}
        {% endblock %}
      </main>


      <!-- CALL-TO-ACTION -->
      <aside class="[ c-cta ] [ max-w-short mt-900 mx-auto py-400 ]">
        <h2 class="[ c-cta__heading ] [ text-700 ]">{{ site.cta.heading }}</h2> 
        <div class="[ c-cta__subheading ] [ text-600 ]">
          {{ site.cta.subheading | markdownFilter | safe }} 
        </div><!-- .c-cta__subheading -->
        <a class="[ c-underline ] [ mt-300 ] [ font-serif font-semibold text-600 ]" href="{{ site.cta.linkTarget }}">{{ site.cta.linkText }}</a>
      </aside>


      <!-- FOOTER -->
      <footer class="[ c-sitefooter ] [ mt-1000 ]">

        <div class="[ c-sitefooter__nav ] [ o-full-bleed ] ">
          <ul class="[ list-reset font-semibold ]">
            <li><a href="mailto:{{ site.email }}">Contact</a></li>
            <li><a href="/writing/">Writing</a></li>
            <li><a href="/feed/writing.xml">RSS</a></li>
          </ul>
        </div><!-- .c-sitefooter__nav -->

        <div class="[ c-sitefooter__location ] [ mt-700 ] [ font-serif italic ]">
          <span>Made with love</span> 
          <span>&nbsp;in Cardiff, Wales</span>
          <img src="/images/dragon.png" width="109" height="72" alt="The Welsh Dragon, a heraldic symbol that appears on the national flag of Wales.">
        </div>

      </footer>

    </div><!-- .o-wrapper -->

    <script>{% include 'js/main.js' %}</script>
  </body>
</html>
